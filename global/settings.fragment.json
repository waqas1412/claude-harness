{
  "$schema": "https://json.schemastore.org/claude-code-settings.json",
  "permissions": {
    "allow": [
      "Bash",
      "WebFetch",
      "WebSearch",
      "Workflow"
    ],
    "deny": [
      "Read(//**/.env)",
      "Read(//**/.env.*)",
      "Read(//**/secrets/**)",
      "Read(//**/*.pem)",
      "Read(//**/*.key)",
      "Read(//**/*.p12)",
      "Read(//**/.netrc)",
      "Read(//**/id_rsa*)",
      "Read(//**/id_ed25519*)",
      "Read(//**/.aws/credentials)",
      "Read(~/.ssh/id_*)",
      "Read(~/.ssh/*_key)"
    ]
  },
  "attribution": {
    "commit": "",
    "pr": ""
  },
  "skipWorkflowUsageWarning": true,
  "_personalPrefs": {
    "_comment": "Keys under _personalPrefs are applied at top level by install.sh ONLY when run without --no-prefs. They are personal-global preferences, not portable defaults. Model and effort are deliberately absent: the /model and /effort pickers own those keys, and listing them here made a plain reinstall silently revert an in-session choice.",
    "theme": "dark",
    "tui": "fullscreen"
  }
}
