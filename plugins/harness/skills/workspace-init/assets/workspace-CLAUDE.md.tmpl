# Workspace Index

> Read this first; do not blind-recurse the repos. Every repo has a deep index at `.claude/repo-index/<repo>.md`. These are NOT auto-loaded: Read the one matching your target repo before editing it. Section 1 is the catalog; Sections 2-3 give deeper routing for the daily-driver repos.

## 1. System Topology

### Core daily-drivers (deep routing in Sections 2-3)
{{#each dailyDrivers}}
- **`{{slug}}`:** {{stackTags}}.
{{/each}}

### Full repo catalog
> One line per repo; open `.claude/repo-index/<repo>.md` for its deep index. Vendored forks are black-box: do not deep-recurse them.

{{#each categories}}
**{{name}}**
{{#each repos}}
- **`{{slug}}`** {{oneLiner}}{{#if vendored}} (vendored fork; black-box){{/if}}
{{/each}}

{{/each}}
> Not indexed (empty or non-repo dirs): {{notIndexed}}.

## 2. Structural Routing Triggers
*Deep routing for the core daily-driver repos. For any other repo, open its `.claude/repo-index/<repo>.md`.*
{{#each routingTriggers}}
- {{concern}}: {{path}}.
{{/each}}
- Per-repo agent rules: read each repo's own `AGENTS.md`/`CLAUDE.md` where present; it is authoritative for that repo.

## 3. Search & Grep Optimization
- **File Patterns:** {{filePatterns}}.
- **Legacy (avoid for new work):** {{legacyDirs}}.
- **Global Deny Rules:** ignore {{denyBuildDirs}} to preserve tokens.
- **Global Deny Rules:** ignore {{denyCacheDirs}}.
- **Never grep-recurse (huge / vendored):** {{neverRecurse}}. Use each repo's deep index instead.
- **Non-code zones (docs/data, not source):** {{nonCodeZones}}; read only when the task explicitly references them.

## 4. Deep Index Pointers
- Deep indexes live in `.claude/repo-index/` (one `<repo>.md` per repo). Read on demand, not auto-loaded: Read the single file matching the repo you touch. The `targets` glob documents scope and feeds tooling; it does not drive loading. Section 1 is the full catalog.
- Core: {{coreDeepIndexes}}. Every other repo: `.claude/repo-index/<repo>.md`.
- **Tracker prefixes by domain:**
{{#each trackerPrefixes}}
  - `{{prefix}}` {{domain}}
{{/each}}

## 5. Deeper navigation (on-demand)
- Read `.claude/meta/navigation.md` for the doc map, the agent-instruction hierarchy, and the agent/workflow/skill guide.
- Read `.claude/meta/integration-map.md` for the cross-repo seam graph and feature-flow playbooks (built by `/refresh-seams`); machine-readable edges in `.claude/meta/seams.json`. Each connected repo's `.claude/repo-index/<repo>.md` also carries an auto-generated `## Cross-repo seams` section.
- Read `.claude/harness/seams/clusters.json` for the cluster and hunter parameters `/refresh-seams` consumes.
- Agent and skill descriptions are injected each turn; pick by them.
